# Fine Dining Coach – Render Web Service (Docker)

- `index.html` is the single-file app (no build step).
- `Dockerfile` runs NGINX and serves `index.html` on port 80.
- Deploy to Render as a **Web Service** (no YAML needed).

